import { Flex } from "@chakra-ui/react";
import styled from "styled-components";

const StyledLogo = styled(Flex)`
  align-items: center;
  justify-content: center;
  padding: 0;
`;
export const Styled = { Logo: StyledLogo };
