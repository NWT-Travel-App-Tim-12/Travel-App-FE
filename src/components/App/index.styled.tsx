import styled from "styled-components";
import { Box } from "@chakra-ui/react";

const StyledApp = styled(Box)`
  margin: 0;
  padding: 0;
  height: 100vh;
`;

export const Styled = { App: StyledApp };
